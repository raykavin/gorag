# Examples

## Basic

A self-contained example showing:
- dense retrieval mode
- reranker retrieval mode
- prompt context block returned by `engine.RAGEngine`

Run:

```bash
go run ./examples/basic
```
